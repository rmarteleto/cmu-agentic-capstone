# --- Core orchestration & agents (per M4 tool mapping) ---
crewai>=0.51.0              # Thought Generator + Compliance Critic agents
langchain>=0.2.0           # Orchestrator / beam-search controller
langchain-core>=0.2.0
langchain-openai>=0.1.0    # LLM + embeddings adapter (OpenAI)
langchain-anthropic>=0.1.0 # alternative LLM backend

# --- Retrieval / memory ---
chromadb>=0.5.0            # vector database (SOP + SIG corpora)
rank-bm25>=0.2.2           # keyword arm of hybrid search
sentence-transformers>=3.0.0  # local cross-encoder reranker (optional)

# --- Spreadsheet I/O ---
openpyxl>=3.1.0

# --- Utilities ---
pydantic>=2.6.0
python-dotenv>=1.0.0
tenacity>=8.2.0

# --- Dev / test ---
pytest>=8.0.0

# --- Serving / Copilot Studio integration ---
fastapi>=0.110.0           # REST API for Power Platform custom connector
uvicorn>=0.29.0            # ASGI server
mcp>=1.2.0                 # Model Context Protocol server (Copilot Studio tool)
